import {storiesOf} from '@storybook/react';

storiesOf('Patterns', module)
    .add('Patterns', () => {})
    .add('Ask users for…', () => {})
    .add('Help users to…', () => {})
    .add('Pages', () => {});
